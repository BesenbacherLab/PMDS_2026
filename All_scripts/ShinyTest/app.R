library(tidyverse)
library(shiny)
library(bslib)

# Define UI ----
ui <- ?

# Define server logic ----
server <- function(input, output) {
  ?
}

# Run the app ----
shinyApp(ui = ui, server = server)
